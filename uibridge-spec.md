# UIBridge 技術仕様書

## 0\. この仕様書の位置づけ

本ドキュメントは UIBridge の第1版（MVP）を Claude Code などのAI開発支援ツールに引き渡すための技術仕様書である。

**開発中の柔軟性について:** 本仕様書は開発の出発点であり、絶対的な設計図ではない。実装フェーズにおいて以下のような判断が生じた場合、仕様書に固執せず柔軟に検討・変更してよい。

- 実装してみて明らかに使いにくいUIになった場合  
- 技術的により良いアプローチが見つかった場合  
- 依存ライブラリの制約や挙動により、仕様通りの実装が困難な場合  
- スマホ環境での実機検証で問題が判明した場合  
- 開発途中で新しいアイデアが浮かんだ場合

変更が生じた場合は、その意図と経緯をコミットメッセージまたはREADMEに記録すること。仕様書自体も生きたドキュメントとして随時更新してよい。**「動くもの」＞「仕様書通り」** を原則とする。

---

## 1\. プロジェクト概要

### 1.1 ツール名

**UIBridge**

### 1.2 目的

AIを利用したアプリ開発において、UIの見た目を自然言語で伝えても意図通りの結果が得られない問題を解決する。ユーザーが視覚的にUIパーツを調整し、AIが正確に理解できる形式で出力できるツールを提供する。

### 1.3 コンセプト

**ツール自体がライブプレビューになる** UI編集ツール。プレビュー用の枠を別途持たず、ユーザーが調整している対象がそのままツールのUI要素として反映される。

### 1.4 ユースケース

- 個人開発でお気に入りのボタンデザインを作り、複数プロジェクトで使い回す  
- AIコーディング支援ツール（Claude Code等）に「このデザインで作って」と正確に伝える  
- ネットで見つけたデザインをベースに微調整する（Phase 2）

### 1.5 利用形態

- 個人利用  
- ブラウザ完結（バックエンドなし）  
- GitHub Pages で配布  
- スマホ環境で動作検証可能

---

## 2\. スコープ

### 2.1 対象

- UIパーツ単位の見た目の調整（背景、ボタン、カード、入力欄など）  
- デザイントークン（色、角丸、線幅、影など）の管理  
- 複数フォーマットでの出力

### 2.2 対象外

- レイアウト編集（配置、グリッド、余白調整）  
- 複数画面のフロー設計  
- アニメーション編集（プリセットに含まれる範囲を除く）  
- チーム共有機能

---

## 3\. MVP スコープ（第1版）

### 3.1 MVPに含む機能

1. ビルトインプリセット選択（グラデ、単色、ニューモフィズム等）  
2. 中央バー（コントロールパネル）による色・角丸・線・影の調整  
3. ユーザープリセット保存（localStorage）  
4. 3レイヤー出力  
   - HTML \+ inline CSS  
   - CSS変数テーマ  
   - 自然言語記述  
5. 現在の画面のPNG保存  
6. スマホ対応（レスポンシブ、タッチ操作）  
7. GitHub Pages 配布

### 3.2 Phase 2 以降（後回し）

- CSSペースト取り込み（ハイブリッド方式）  
- Tailwind クラス版出力  
- パーツ拡張（トグル、スライダー、モーダル、ナビゲーション等）  
- プリセットのインポート/エクスポート（JSONファイル）  
- 複数端末同期（Firebase等）

---

## 4\. 全体構成

### 4.1 プラットフォーム

- **配布形態:** 単一 HTML ファイル（または最小構成のHTML/CSS/JS）  
- **ホスティング:** GitHub Pages  
- **リポジトリ管理:** GitHub  
- **必須動作環境:** モバイルブラウザ（iOS Safari, Android Chrome）  
- **推奨動作環境:** 上記 \+ デスクトップブラウザ

### 4.2 技術スタック

- **フロントエンド:** Vanilla JS または軽量フレームワーク（Alpine.js, Preact など検討可）  
- **ビルド:** ビルド不要が理想。必要なら Vite  
- **ストレージ:** localStorage  
- **画像出力:** html2canvas または dom-to-image  
- **外部依存:** できる限り最小（CDN読み込みで完結が理想）

**選定方針:** GitHub Pages で単純にホストするだけで動く構成を優先。ビルドプロセスが必要な場合は GitHub Actions で自動化。

### 4.3 画面構成方針

- 調整UI領域（コントロールパネル）とプレビュー対象領域は視覚的に分離  
- コントロール領域は中立デザインを維持し、ユーザーが極端なテーマを選んでも操作不能にならないようにする  
- プレビュー対象領域が画面の大半を占める

---

## 5\. データモデル

### 5.1 デザイントークン

UIの見た目を構成する基本単位。グローバルテーマとして全体に適用される。

{

  "colors": {

    "primary": "\#667eea",

    "secondary": "\#764ba2",

    "background": "\#ffffff",

    "surface": "\#f5f5f5",

    "text": "\#1a1a1a",

    "textMuted": "\#666666",

    "border": "\#e0e0e0"

  },

  "radius": {

    "sm": 4,

    "md": 8,

    "lg": 12,

    "xl": 20,

    "full": 9999

  },

  "shadow": {

    "sm": "0 1px 2px rgba(0,0,0,0.05)",

    "md": "0 4px 6px rgba(0,0,0,0.1)",

    "lg": "0 10px 25px rgba(0,0,0,0.15)"

  },

  "border": {

    "width": 1,

    "style": "solid"

  },

  "spacing": {

    "unit": 4

  }

}

### 5.2 パーツ別設定

デザイントークンを上書きする形で、パーツ個別の設定を持つ。

{

  "button": {

    "background": {

      "type": "gradient",

      "angle": 135,

      "stops": \[

        { "color": "\#667eea", "position": 0 },

        { "color": "\#764ba2", "position": 100 }

      \]

    },

    "textColor": "\#ffffff",

    "radius": 12,

    "borderWidth": 0,

    "borderColor": "transparent",

    "shadow": "0 4px 15px rgba(102,126,234,0.4)",

    "paddingX": 24,

    "paddingY": 12,

    "fontSize": 16,

    "fontWeight": 600

  },

  "background": {

    "type": "solid",

    "color": "\#ffffff"

  },

  "card": {

    "background": "\#ffffff",

    "borderRadius": 12,

    "borderWidth": 1,

    "borderColor": "\#e0e0e0",

    "shadow": "0 2px 8px rgba(0,0,0,0.06)",

    "padding": 16

  },

  "input": {

    "background": "\#ffffff",

    "borderRadius": 8,

    "borderWidth": 1,

    "borderColor": "\#d0d0d0",

    "textColor": "\#1a1a1a",

    "paddingX": 12,

    "paddingY": 10

  }

}

### 5.3 プリセット

{

  "id": "preset\_neumorphism\_light",

  "name": "ニューモフィズム（ライト）",

  "type": "builtin",

  "createdAt": "2026-01-01T00:00:00Z",

  "tokens": { /\* デザイントークン \*/ },

  "parts": { /\* パーツ別設定 \*/ }

}

### 5.4 ユーザープリセット

ユーザーが保存したプリセット。上記の `type` が `"user"` になる。

### 5.5 localStorage キー設計

- `uibridge:currentState` — 現在編集中の状態  
- `uibridge:userPresets` — ユーザープリセットの配列  
- `uibridge:lastSelectedPreset` — 最後に選択したプリセットID  
- `uibridge:settings` — ツール自体の設定（コントロールUIのダーク/ライト等）

---

## 6\. 主要機能仕様

### 6.1 プリセット選択

- 起動時はデフォルトのシンプルなプリセットが適用される  
- プリセット一覧はビルトイン \+ ユーザープリセットを表示  
- 選択で即座にUIに反映  
- ビルトインプリセット案（初期セット）  
  - シンプル（デフォルト）  
  - グラデーション（紫→青）  
  - グラデーション（オレンジ→ピンク）  
  - 単色フラット  
  - ニューモフィズム（ライト）  
  - ニューモフィズム（ダーク）  
  - ガラスモーフィズム  
  - レトロ（ハードシャドウ）

### 6.2 コントロールパネル（中央バー）

- パーツ選択タブ: 背景 / ボタン / カード / 入力欄  
- 選択したパーツの設定項目をスライダー・カラーピッカー・数値入力で調整  
- 調整は即座にUIに反映  
- スマホでの操作性を考慮し、ボトムシート形式も検討可

### 6.3 プリセット保存

- 現在の状態にユーザーが名前を付けて保存  
- localStorage に追加  
- ユーザープリセットは削除・リネーム可能

### 6.4 出力機能

3つの出力形式をタブまたはボタンで切り替え、クリップボードコピー対応。

**形式A: HTML \+ inline CSS**

\<button style="background: linear-gradient(135deg, \#667eea, \#764ba2); color: \#fff; border: none; border-radius: 12px; padding: 12px 24px; font-size: 16px; font-weight: 600; box-shadow: 0 4px 15px rgba(102,126,234,0.4);"\>

  Click me

\</button\>

**形式B: CSS変数テーマ**

:root {

  \--color-primary: \#667eea;

  \--color-secondary: \#764ba2;

  \--btn-bg: linear-gradient(135deg, var(--color-primary), var(--color-secondary));

  \--btn-radius: 12px;

  \--btn-shadow: 0 4px 15px rgba(102,126,234,0.4);

  /\* ... \*/

}

.btn {

  background: var(--btn-bg);

  border-radius: var(--btn-radius);

  box-shadow: var(--btn-shadow);

  /\* ... \*/

}

**形式C: 自然言語記述**

【ボタン】

\- 背景: 135度のグラデーション、開始色は紫(\#667eea)、終了色は濃い紫(\#764ba2)

\- 文字色: 白

\- 角丸: 12px

\- 影: 下に淡い紫の影（0 4px 15px rgba(102,126,234,0.4)）

\- パディング: 縦12px、横24px

\- フォント: 16px、太さ600

【背景】

\- 単色: 白(\#ffffff)

【カード】

\- 背景: 白

\- 角丸: 12px

\- 枠線: 1px、淡いグレー(\#e0e0e0)

\- 影: 控えめ（0 2px 8px rgba(0,0,0,0.06)）

### 6.5 画面保存（PNG出力）

- 現在のプレビュー領域を PNG として保存  
- html2canvas または dom-to-image を使用  
- スマホでは画像をカメラロールに保存できる形（ダウンロードトリガー）  
- ファイル名: `uibridge_YYYYMMDD_HHmmss.png`

---

## 7\. 画面フロー

### 7.1 起動時

1. localStorage から前回の状態を復元、なければデフォルトプリセット適用  
2. メイン画面表示（プレビュー領域 \+ コントロール入口）

### 7.2 プリセット変更

1. プリセットボタンタップ → 一覧表示  
2. 選択 → 即反映、状態を localStorage に保存

### 7.3 個別調整

1. コントロールパネルを開く  
2. パーツタブ選択 → 該当項目のスライダー等が表示  
3. 調整 → 即反映  
4. 閉じる → 状態を localStorage に保存

### 7.4 プリセット保存

1. 保存ボタン → 名前入力ダイアログ  
2. 保存 → ユーザープリセットに追加

### 7.5 出力

1. 出力ボタン → 3形式のタブ画面  
2. 各形式のコード表示、コピーボタン  
3. PNG保存ボタン別途

---

## 8\. スマホ対応

### 8.1 具体策

- **viewport:** `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">`  
- **タッチターゲット:** 最小 44×44 px（iOS HIG準拠）  
- **セーフエリア対応:** `env(safe-area-inset-*)` を使用  
- **横スクロール禁止:** `overflow-x: hidden` を body に  
- **ボトムシート:** コントロールパネルはスマホでは下から出るシート形式が扱いやすい  
- **タッチ操作:** スワイプでシート開閉、ピンチズームは無効化しない

### 8.2 実機検証項目

- iOS Safari での表示・動作  
- Android Chrome での表示・動作  
- 縦横回転時のレイアウト崩れ  
- PNG保存機能の動作  
- クリップボードコピーの動作（HTTPS必須の場合あり）

---

## 9\. GitHub 連携

### 9.1 リポジトリ構成

uibridge/

├── index.html           \# エントリポイント

├── assets/

│   ├── css/

│   ├── js/

│   └── presets/         \# ビルトインプリセットJSON

├── docs/

│   └── uibridge-spec.md \# 本仕様書

├── .github/

│   └── workflows/       \# 必要ならデプロイ用

└── README.md

### 9.2 デプロイ

- **GitHub Pages 設定:** リポジトリの Settings → Pages で `main` ブランチのルート、または `docs/` を公開  
- **ビルド不要な構成が理想。** Vite等を使う場合は GitHub Actions で自動ビルド・デプロイ

### 9.3 動作確認URL

- `https://<username>.github.io/uibridge/`  
- スマホから直接アクセスして検証

---

## 10\. Phase 2 で検討する機能

### 10.1 CSSペースト取り込み（ハイブリッド方式）

- ペーストしたCSSをそのまま`<style>`として適用（見た目は必ず再現）  
- 認識できたプロパティ（`background`, `border-radius`, `padding`, `box-shadow`など）は内部モデルに取り込み、スライダーで調整可能に  
- 認識できなかった部分（`::before` 擬似要素、複雑なアニメーション等）は「編集不可レイヤー」として保持、UI上でロックアイコン表示  
- CSSパーサライブラリの選定必要（PostCSS 等）

### 10.2 その他候補

- Tailwind クラス版出力  
- パーツ拡張  
- プリセットのインポート/エクスポート（JSONファイル）  
- Firebase 連携での複数端末同期  
- コミュニティプリセット共有（他ユーザーの作品をインポート）

---

## 11\. 開発の進め方（推奨）

1. **リポジトリ作成 \+ GitHub Pages 有効化**（骨組みだけでも公開状態にする）  
2. **デフォルトプリセットで静的に動くページを作る**  
3. **プリセット切り替え実装**  
4. **コントロールパネル実装（1パーツから）**  
5. **他パーツに拡張**  
6. **localStorage 保存・復元**  
7. **ユーザープリセット保存**  
8. **出力機能（3形式）**  
9. **PNG保存**  
10. **スマホ実機検証と調整**

各ステップで実機確認しながら進めることを推奨。仕様に無理があれば適宜変更してよい（**冒頭0章参照**）。

---

## 12\. 未確定事項

以下は開発中に決定してよい項目。

- フレームワーク選定（Vanilla / Alpine / Preact 等）  
- ビルド構成の要否  
- コントロールパネルのUIパターン（サイドバー / ボトムシート / モーダル）  
- ビルトインプリセットの最終ラインナップと具体的な数値  
- 自然言語記述のフォーマット詳細（項目の並び順、日本語/英語切り替え等）  
- カラーピッカー等のUIパーツの実装方法（ネイティブ input type=color / カスタム実装 / ライブラリ）

---

以上  
